<UserControl x:Class="CosmoWhisper.Views.MicrophoneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Microphone Configuration" FontSize="24" Foreground="White" Margin="0,0,0,20" FontWeight="Bold"/>
            
            <Border Background="#20FFFFFF" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Input Device" Foreground="#8FFF" Margin="0,0,0,10"/>
                    <ComboBox x:Name="ComboMics" Background="#30FFFFFF" BorderThickness="0" Foreground="White" Padding="10"/>
                </StackPanel>
            </Border>

            <Border Background="#20FFFFFF" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Input Sensitivity" Foreground="#8FFF" Margin="0,0,0,10"/>
                    <Slider x:Name="SldSensitivity" Minimum="0" Maximum="100" Value="50" TickFrequency="1" IsSnapToTickEnabled="True"/>
                    <TextBlock x:Name="TxtSensitivityValue" Text="50%" Foreground="White" HorizontalAlignment="Right" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <Border Background="#20FFFFFF" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="Silence Threshold" Foreground="#8FFF" Margin="0,0,0,10"/>
                    <ProgressBar x:Name="LiveMonitorBar" Height="10" Background="#10FFFFFF" Foreground="#007AFF" Maximum="100" Value="0" BorderThickness="0"/>
                    <TextBlock Text="Auto-stop recording after 2s of silence" Foreground="#6FFF" FontSize="12" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
