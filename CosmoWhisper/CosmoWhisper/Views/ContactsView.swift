import SwiftUI

struct ContactsView: View {
    var body: some View {
        VStack {
            Text("Feature Removed")
                .font(.headline)
            Text("This view is no longer in use.")
                .font(.subheadline)
                .foregroundColor(.secondary)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
